# Install

## Installation

To install ApiUtils, simply use npm:

```bash
$ npm install apiutils.js
```

OR

```bash
$ yarn add apiutils.js
```

Make sure you have [Node.js](https://nodejs.org/en/download/) installed, with Node.js 14 or higher being required. If you're starting a new project, initialize it with `npm init` before installing ApiUtils.